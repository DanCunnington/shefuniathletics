<!DOCTYPE html>
<html>
<head>
  <title>University of Sheffield Athletics and XC Club</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>

  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
  <link href='http://fonts.googleapis.com/css?family=Montserrat:400,700' rel='stylesheet' type='text/css'>
  <link href='http://fonts.googleapis.com/css?family=Lato&subset=latin,latin-ext' rel='stylesheet' type='text/css'>
  <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.3.0/css/font-awesome.min.css">
  <%= csrf_meta_tags %>
  <%=favicon_link_tag(source='favicon.ico', options={})%>
  <%= render 'layouts/shim' %>
</head>
<body style= "background: url(<%=root_url+'bgOpacity.png'%>) top right, url(<%=@backgroundImageURL%>) top right no-repeat;

    
    background-position: center;
    background-attachment: fixed;
    background-size: cover;
    background-color: #131313; 
    height: 100%;
    color: whitesmoke;
    margin-top: 50px;" <%="onload=#{@onload}"%>>
 

<%= render 'layouts/nav' %>
<%= cloudinary_js_config %>

<div class="container">

<%= render 'layouts/chooseImageModal' %>

<% flash.each do |key, value| %>
        <div class="alert alert-<%= key %>"><%= value %></div>
      <% end %>

	<%= yield %>
<%= render 'layouts/footer' %>

</div>
</body>
</html>
