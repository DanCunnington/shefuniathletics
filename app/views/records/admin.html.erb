<h1 class="heading">Club Records</h1>
<%= render 'layouts/admin_dash' %>


<div class="signup_cont two-em-spaced">
  <%= link_to "New Record", new_record_path, id: "new_fix", class: "btn btn-block btn-large btn-warning", "data-no-turbolink" => true %>
</div>

<h4 class="montserrat gold">Male Outdoor</h4>
<% if @male_outdoors != [] %> 
<div class="panel panel-default table-responsive">
  <table class="table table-hover">  
    <thead>  
      <tr>    
        <th>Event</th>
        <th>Athlete</th>
        <th>Time</th>
        <th>Competition</th>
        <th></th>
        <th></th>  
      </tr>  
    </thead>  
    <tbody>  
     <% @male_outdoors.each do |record| %>
     <tr>
      <td><%= record.event %></td>
      <td><%= record.athlete %></td>
      <td><%= record.time %></td>
      <td><%= record.competition %></td>  
      <td><%= link_to 'Edit', edit_record_path(record), class:"btn btn-info edit-btn" %></td>
      <td><%= link_to 'Delete', record, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger edit-btn" %></td>
    </tr>
    <% end %>
  </tbody>
</table>  
</div>
<% else %>
<p>Male Outdoor Records will appear shortly.</p>
<% end %>

<h4 class="montserrat gold">Male Indoor</h4>
<% if @male_indoors != [] %> 
<div class="panel panel-default table-responsive">
  <table class="table table-hover">  
    <thead>  
      <tr>    
        <th>Event</th>
        <th>Athlete</th>
        <th>Time</th>
        <th>Competition</th>
        <th></th>
        <th></th>  
      </tr>  
    </thead>  
    <tbody>  
     <% @male_indoors.each do |record| %>
     <tr>
      <td><%= record.event %></td>
      <td><%= record.athlete %></td>
      <td><%= record.time %></td>
      <td><%= record.competition %></td>  
      <td><%= link_to 'Edit', edit_record_path(record), class:"btn btn-info edit-btn" %></td>
      <td><%= link_to 'Delete', record, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger edit-btn" %></td>
    </tr>
    <% end %>
  </tbody>
</table>  
</div>
<% else %>
<p>Male Indoor Records will appear shortly.</p>
<% end %>

<h4 class="montserrat gold">Female Outdoor</h4>
<% if @female_outdoors != [] %> 
<div class="panel panel-default table-responsive">
  <table class="table table-hover">  
    <thead>  
      <tr>    
        <th>Event</th>
        <th>Athlete</th>
        <th>Time</th>
        <th>Competition</th>
        <th></th>
        <th></th>  
      </tr>  
    </thead>  
    <tbody>  
     <% @female_outdoors.each do |record| %>
     <tr>
      <td><%= record.event %></td>
      <td><%= record.athlete %></td>
      <td><%= record.time %></td>
      <td><%= record.competition %></td>  
      <td><%= link_to 'Edit', edit_record_path(record), class:"btn btn-info edit-btn" %></td>
      <td><%= link_to 'Delete', record, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger edit-btn" %></td>
    </tr>
    <% end %>
  </tbody>
</table>  
</div>
<% else %>
<p>Female Outdoor Records will appear shortly.</p>
<% end %>

<h4 class="montserrat gold">Female Indoor</h4>
<% if @female_indoors!= [] %> 
<div class="panel panel-default table-responsive">
  <table class="table table-hover">  
    <thead>  
      <tr>    
        <th>Event</th>
        <th>Athlete</th>
        <th>Time</th>
        <th>Competition</th>
        <th></th>
        <th></th>  
      </tr>  
    </thead>  
    <tbody>  
     <% @female_indoors.each do |record| %>
     <tr>
      <td><%= record.event %></td>
      <td><%= record.athlete %></td>
      <td><%= record.time %></td>
      <td><%= record.competition %></td>  
      <td><%= link_to 'Edit', edit_record_path(record), class:"btn btn-info edit-btn" %></td>
      <td><%= link_to 'Delete', record, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger edit-btn" %></td>
    </tr>
    <% end %>
  </tbody>
</table>  
</div>
<% else %>
<p>Female Indoor Records will appear shortly.</p>
<% end %>

<div class="signup_cont two-em-spaced">
  <%= link_to "New Record", new_record_path, id: "new_fix", class: "btn btn-block btn-large btn-warning", "data-no-turbolink" => true %>
</div>